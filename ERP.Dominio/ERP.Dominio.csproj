<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>ERP.Dominio</RootNamespace>
  </PropertyGroup>

  <!-- El dominio no referencia NADA: ni paquetes de infraestructura, ni ASP.NET, ni EF Core.
       Es la única capa sin dependencias salientes. Ver docs/arquitectura.md. -->

</Project>
